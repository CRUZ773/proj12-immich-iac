# ---------------------------------------------------------------
# DO NOT commit real values to Git.
# This file is a template — fill in base64-encoded values and
# apply manually, OR let the Ansible playbook generate and apply
# secrets automatically (recommended approach).
#
# To base64-encode a value:  echo -n 'your-value' | base64
# ---------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: immich-secrets
  namespace: immich
type: Opaque
data:
  # base64-encoded PostgreSQL password (default: "immich_password")
  db-password: aW1taWNoX3Bhc3N3b3Jk
  # base64-encoded JWT secret — replace with a long random string
  jwt-secret: Y2hhbmdlbWVpbXBsZWFzZXN0cmlwdGhlYWRtaW4=
